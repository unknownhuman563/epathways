// Thin wrapper so the shared notifications page picks up this portal's
// layout via app.jsx's component-name prefix auto-wrapping. The real UI
// (filters, list, pagination, actions) lives in pages/notifications/Index.
export { default } from "@/pages/notifications/Index";
