// Immigration uses the same Documents UX as Education — person-attached
// folders, shared queue. This is a thin re-export so we maintain one file.
export { default } from "@/pages/portal/education/Documents";
