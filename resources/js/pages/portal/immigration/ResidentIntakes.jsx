// Re-export of the admin Resident Intakes page so immigration-role users
// (and the auto-layout resolver in app.jsx) get the Immigration Portal chrome.
export { default } from '@/pages/admin/Immigration/ResidentIntakes';
