// Thin wrapper so the shared "My Tickets" page picks up this portal's
// layout via app.jsx's component-name prefix auto-wrapping. Real UI lives
// in pages/tickets/MyTickets.
export { default } from "@/pages/tickets/MyTickets";
