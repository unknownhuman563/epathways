// The adviser portal reuses the immigration Visas page verbatim; only the path
// prefix differs, so the app resolver wraps it in the adviser layout instead.
export { default } from "@/pages/portal/immigration/VisaTypes";
