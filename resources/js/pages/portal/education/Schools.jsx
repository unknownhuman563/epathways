// Re-export of admin Schools — resolved under portal/education/ so
// app.jsx wraps it in EducationLayout instead of AdminLayout when the
// education team hits /portal/education/schools.
export { default } from '@/pages/admin/Schools';
