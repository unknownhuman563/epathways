// Re-export of admin SchoolProfile — resolved under portal/education/ so
// app.jsx wraps it in EducationLayout instead of AdminLayout when the
// education team opens a school profile.
export { default } from '@/pages/admin/SchoolProfile';
