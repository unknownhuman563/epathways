// Re-export of the admin User Reviews page, scoped to the education
// department. Same React component the admin sees, rendered under the
// EducationLayout chrome because the page path starts with portal/education/.
export { default } from '@/pages/admin/Education/UserReviews';
